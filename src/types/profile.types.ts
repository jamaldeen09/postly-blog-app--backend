// ** These types are for defining the structure of data that is used by the current user ** \\

interface UserProfile {
    _id: string;
    username: string;
    email: string;
    createdAt: string;
    updatedAt: string;
};


export { UserProfile }